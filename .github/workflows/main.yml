name: Build Mod
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x' # Adjust if the mod uses a different version

      - name: Restore Dependencies
        run: dotnet restore --configuration Android

      - name: Build DLL
        run: dotnet build --configuration Android

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AmongUsRevamped-Fixed
          path: bin/Release/**/*.dll

